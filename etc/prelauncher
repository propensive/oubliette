BIN=%%BIN%%;([ -f "$HOME/.cache/.%%BIN%%" ] || (mkdir -p "$HOME/.cache" && wget -qO "$HOME/.cache/.%%BIN%%" %%URL%% || curl -sL %%URL%% > "$HOME/.cache/.%%BIN%%")) && [ $(md5sum "$HOME/.cache/.%%BIN%%" | head -c32) = %%MD5%% ] && sh $HOME/.cache/.%%BIN%% || printf "Unable to launch %%NAME%%.\n"
